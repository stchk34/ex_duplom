{#
/**
 */
#}
{{ form.messages }}
<div class="poll">
  <div class="vote-form">
    <h3 class="poll-question">{{ question }}</h3>

    {{ form.choice }}

    {{ form.actions }}
  </div>
  {{ form|without('actions', 'choice', 'messages', 'question') }}
</div>
